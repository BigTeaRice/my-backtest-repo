name: Daily Backtest and Deploy

on:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 22 * * *'  # UTC 22:00 = 北京 06:00
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  run-backtest:
    runs-on: ubuntu-latest

    steps:
    # 1. 检出源码
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. 源码安装 TA-Lib（Ubuntu 24.04 通用）
    - name: Build & install TA-Lib from source
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential wget
        wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
        tar -xzf ta-lib-0.4.0-src.tar.gz
        cd ta-lib
        ./configure --prefix=/usr/local
        make && sudo make install
        sudo ldconfig
        cd .. && rm -rf ta-lib*

    # 3. Python 环境
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.9"
        cache: 'pip'

    # 4. 安装 Python 依赖（含 TA-Lib）
    - name: Install Python dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    # 5. 创建输出目录
    - name: Create output directories
      run: mkdir -p public/reports

    # 6. 运行回测
    - name: Run backtest
      run: |
        echo "开始运行多策略回测（TA-Lib 版）..."
        python main.py

    # 7. 列出生成文件（存在才显示，避免中断）
    - name: List generated files
      run: |
        echo "📊 生成的报告文件："
        find public -name "*.html" | head -10
        echo "📊 CSV 报告："
        [ -f public/strategy_comparison.csv ] && ls -l public/strategy_comparison.csv || echo "⚠️ 暂无 CSV"

    # 8. 上传 GitHub Pages 构件
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './public'

  deploy:
    needs: run-backtest
    if: success()

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4

    - name: Show deployment info
      run: |
        echo "🌐 部署完成！"
        echo "网站地址：${{ steps.deployment.outputs.page_url }}"
