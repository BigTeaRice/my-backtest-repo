name: Backtest and Deploy

on:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 22 * * *'  # 每天 UTC 22:00
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  run-backtest:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.9"
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run backtest
      run: |
        echo "开始运行回测..."
        python main.py
        
        echo "检查生成的文件..."
        ls -la public/
        ls -la public/reports/ || echo "reports目录可能不存在"
        
        if [ -f "public/index.html" ]; then
          echo "✅ 主页面生成成功"
          echo "📊 报告数量: $(find public/reports -name '*.html' 2>/dev/null | wc -l || echo 0)"
        else
          echo "❌ 主页面生成失败"
          exit 1
        fi
    
    - name: Upload Pages Artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './public'
  
  deploy:
    needs: run-backtest
    if: success()
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    runs-on: ubuntu-latest
    
    permissions:
      pages: write
      id-token: write
    
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
    
    - name: Show deployment URL
      run: |
        echo "🌐 网站已部署: ${{ steps.deployment.outputs.page_url }}"
        echo ""
        echo "📋 使用方法:"
        echo "1. 打开上面的链接"
        echo "2. 选择策略和股票标的"
        echo "3. 点击'加载回测报告'"
        echo "4. 查看图表和统计数据"
